import type { WorkItem } from "@/data/workItems/types";

// Featured is controlled by each work item's featured boolean. Keep this
// module empty so featured projects are not duplicated.
export const featuredWorks: WorkItem[] = [];
