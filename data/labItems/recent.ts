import type { LabItem } from "@/data/labItems/types";

// Recent is a cross-listing category. Add "recent" to a lab item's
// categories in its primary module instead of duplicating it here.
export const recentLabs: LabItem[] = [];
